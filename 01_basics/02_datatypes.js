"use strict" // treat all JS code as newer version

// alert(3+3) // it doesn't work, because we are using nodejs, not browser javascrit

console.log(3
    +
    3
) // code readbility should be high priority

console.log("Hitesh");

// data types
let name = "hitesh" // string
let age = 18 // number
let isLoggedIn = false // boolean
let state = null

// number => 2^53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined
// symbol => for unique values

// object


console.log(typeof(undefined)); //undefined
console.log(typeof(null)); // object